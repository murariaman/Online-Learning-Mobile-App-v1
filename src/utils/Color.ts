const Colors = {
    background: (a = 1) => {
        return `rgba(242, 242, 242, ${a})`;
    },
    darkColor: (a = 1) => {
        return `rgba(80, 121, 242, ${a})`;
    },
    black: (a = 1) => {
        return `rgba(32, 32, 35, ${a})`;
    },
    white: (a = 1) => {
        return `rgba(255, 255, 255, ${a})`;
    },
    appWhite: (a = 1) => {
        return `rgba(242, 242, 242, ${a})`;
    },
    darkBlue: (a = 1) => {
        return `rgba(75, 131, 242, ${a})`;
    },
    F9Background: (a = 1) => {
        return `rgba(249, 249, 249, ${a})`;
    },
    extremeBlue: (a = 1) => {
        return `rgba(21, 49, 81, ${a})`;
    },
    shadowWhite: (a = 1) => {
        return `rgba(230, 230, 230, ${a})`;
    },
    blueShadow: (a = 1) => {
        return `rgba(130, 168, 217, ${a})`;
    },
    headerBlue: (a = 1) => {
        return `rgba(75, 131, 242, ${a})`;
    },
    headerFontColor: (a = 1) => {
        return `rgba(242, 242, 242, ${a})`;
    }
}

export default Colors;